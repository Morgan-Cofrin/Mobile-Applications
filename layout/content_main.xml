<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              xmlns:tools="http://schemas.android.com/tools"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:paddingBottom="@dimen/activity_vertical_margin"
              android:paddingLeft="@dimen/activity_horizontal_margin"
              android:paddingRight="@dimen/activity_horizontal_margin"
              android:paddingTop="@dimen/activity_vertical_margin"
              app:layout_behavior="@string/appbar_scrolling_view_behavior"
              tools:context=".Activities.Main.MainActivity"
              tools:showIn="@layout/activity_main">

    <GridLayout
        android:layout_width="182dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp">

        <Button
            android:id="@+id/btn_allTerms"
            android:layout_width="180dp"
            android:layout_height="154dp"
            android:layout_row="0"
            android:layout_column="0"
            android:contentDescription="@string/view_all_terms"
            android:onClick="openTermList"
            android:padding="30dp"
            android:scaleType="fitCenter"
            android:text="View All Terms"
            tools:text="View All Terms" />

    </GridLayout>

    <Button
        android:id="@+id/btn_currentTerm"
        android:layout_width="180dp"
        android:layout_height="162dp"
        android:onClick="openCurrentTerm"
        android:padding="30dp"
        android:scaleType="fitCenter"
        android:text="View Current Term"
        tools:text="View Current Term" />

</LinearLayout>